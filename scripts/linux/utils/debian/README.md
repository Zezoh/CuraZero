## Installation instructions

There are two ways to install the package.

* Using apt :
```
cp [mypath]/CuraZero_<BUILD_ARCHITECTURE>.deb /var/cache/apt/archives/
sudo apt install curazero
```
or
```
sudo apt install [mypath]/CuraZero_<BUILD_ARCHITECTURE>.deb
```

* Using dpkg :
```
sudo dpkg -i [mypath]/CuraZero_<BUILD_ARCHITECTURE>.deb
sudo apt install -f
```
